import 'package:flutter/material.dart';

class RelaxEdition extends StatefulWidget {
  const RelaxEdition({Key? key}) : super(key: key);
  @override
  State<RelaxEdition> createState() => _RelaxEditionViewState();
}

class _RelaxEditionViewState extends State<RelaxEdition> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
